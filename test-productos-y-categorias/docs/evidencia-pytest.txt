# Evidencia de ejecución

Entorno: Python 3.12, FastAPI 0.141.1, pytest 9.1.1, TestClient y base de datos en memoria.

## Suite completa

```text
$ .venv/bin/python -m pytest -q
........................................                                 [100%]
40 passed, 2 warnings in 0.65s
```

Los 40 casos incluyen los 34 casos originales y 6 regresiones nuevas para nombres en blanco, nombre nulo en PATCH, precios no finitos y campos desconocidos.

## Verificaciones adicionales

```text
$ .venv/bin/python -m compileall -q app test
OK

GET /       -> 200
GET /health -> 200
GET /docs   -> 200
GET /openapi.json -> 200
```

Las dos advertencias son deprecaciones emitidas por la combinación actual de Starlette, httpx y anyio dentro de `TestClient`; no representan fallos de la API ni hacen fallar la suite.
